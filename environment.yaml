# Thin conda shell: conda supplies python + pip, requirements.txt is the
# SINGLE source of truth for dependencies (pip-only clusters install the
# same file directly — see its header for the venv recipe).
name: jrt

channels:
  - conda-forge
  - defaults

dependencies:
  - python>=3.12
  - pip

  - pip:
      - -r requirements.txt
